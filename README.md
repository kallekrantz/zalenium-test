This is a zalenium test-bench to put it under extensive load under a longer period of time.

To run, execute the following command:
```
docker-compose up --build --force-recreate --scale selenium-test=4
```